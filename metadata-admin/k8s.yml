apiVersion: apps/v1
kind: Deployment
metadata:
  name: "metadata-admin"
spec:
  selector:
    matchLabels:
      app: "metadata-admin"
  template:
    metadata:
      labels:
        app: "metadata-admin"
    spec:
      containers:
        - name: "metadata-admin"
          image: "gcr.io/metadata-admin/jib-image"
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 3
            failureThreshold: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "metadata-admin"
spec:
  selector:
    app: "metadata-admin"
  type: LoadBalancer
  ports:
    - protocol: "TCP"
      port: 8080